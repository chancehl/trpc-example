{
  "name": "@chancehl/trpc-example",
  "version": "1.0.0",
  "scripts": {
    "dev:server": "tsx watch server",
    "dev:client": "wait-port 8080 && tsx watch client",
    "start": "node dist/server",
    "build": "tsc"
  },
  "workspaces": [
    "client",
    "server"
  ],
  "devDependencies": {
    "@types/node": "^18.11.8",
    "concurrently": "^7.5.0",
    "tsx": "^3.11.0",
    "typescript": "^4.8.4",
    "wait-port": "^1.0.4"
  },
  "dependencies": {
    "node-fetch": "^3.2.10"
  }
}
